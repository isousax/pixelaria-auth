# Configurações de Ambiente - auth-engine
# Copie este arquivo para .env e preencha com seus valores

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRATION_SEC=3600

# Refresh Token Configuration
REFRESH_TOKEN_EXPIRATION_DAYS=30

# CORS Configuration
CORS_ORIGINS=http://pixelaria.com.br,https://app.pixelaria.com.br

# Environment
ENVIRONMENT=development

# Email Configuration (se aplicável)
SMTP_HOST=smtp.pixelaria.com.br
SMTP_PORT=587
SMTP_USER=noreply@pixelaria.com.br
SMTP_PASSWORD=your-smtp-password

# Cloudflare Workers
# (configurado via wrangler.jsonc - não precisa estar aqui)
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
